# M6 — Cross-session recall + fail-open integration tests

## G0 · 2026-08-20T18:38+05:30
- wiki pages read: [[concepts/FailOpenRetrieve]], index.md sources (Phase 14/16 from Deepseek_Harness_plugin_with_remex.md)
- implementation-notes searched for: "failure.test.ts", "recall-experiment" — none live
- codebase grep: `tests/failure.test.ts`, `tests/integration/recall-experiment.test.ts` — 0 hits (unbuilt)
- decisions that bind us: mock stopped API via RemexClient+Provider; scripted Teja/driving/dosa with in-memory store
- verdict: **UNBUILT**

## iter 1 · 2026-08-20T18:39+05:30
- gate G1: pass · skills considered [agentic-swe-master, tdd, production-readiness]; chose tdd + production-readiness
- gate G2: pass · +2 test files, 11 new tests green
- gate G3: pass · single BUILD iter
- gate G4: pass · demo exit 0:
  ```bash
  pnpm test tests/failure.test.ts  # 7/7
  pnpm test tests/integration/recall-experiment.test.ts  # 4/4
  pnpm exec tsc --noEmit  # exit 0
  pnpm test  # 44/44
  ```
- gate G5: deferred → L4 below

## L4 VERIFY · 2026-08-20T18:46+05:30

**Context (fresh — no build trail):**
- Goal: `failure.test.ts` proves empty retrieve when Remex stopped; agent continues; Teja/driving/dosa experiment documents cross-session recall (scripted or manual).
- Artifact: `tests/failure.test.ts`, `tests/integration/recall-experiment.test.ts`
- Invariants at risk: `retrieve_fail_open` (primary), `plugin_boundary` (tests only — no new src), `write_non_blocking` (N/A)

**Computed gates:**
```bash
pnpm test tests/failure.test.ts                      # exit 0 · 7/7
pnpm test tests/integration/recall-experiment.test.ts # exit 0 · 4/4
pnpm exec tsc --noEmit                                # exit 0
pnpm test                                            # exit 0 · 44/44
```

**Invariant check:**
| Invariant | Verdict |
|-----------|---------|
| `plugin_boundary` | PASS — M6 adds tests only; imports `src/**`, `@deepseek-ai/cordis`, `@deepseek-ai/dsh-llm` |
| `retrieve_fail_open` | PASS — failure tests wire RemexClient→Provider→handlePreStepInjection; 503/fetch/degraded → EMPTY_RECALL; no throw to agent path |
| `write_non_blocking` | N/A — read-path milestone |

**Success criteria check:**
| Criterion | Verdict |
|-----------|---------|
| Empty retrieve when API stopped | PASS — 503 + connection refused + degraded covered |
| Agent continues with empty memory | PASS — pre-step returns unchanged `enter` decision, no `<remex_memory>` |
| Cross-session recall documented | PASS — recall-experiment header documents manual steps; 4 scripted tests |
| Teja/driving/dosa sequence | PASS — turn 2 recalls driving; turn 3 ranks driving over dosa |
| Demo command green | PASS (7/7) |

**Verdict: APPROVE**

**Quiz-me gate (verifier → human):**
1. **Design:** Why does `failure.test.ts` test provider + `handlePreStepInjection` instead of only `RemexMemoryProvider.recall`?
2. **Edge case:** What happens to pre-step when Remex returns HTTP 200 with `degraded: true`?
3. **Impact:** Why is the cross-session experiment scoped by tenant+user rather than DSH session id?

**Suggested answers (for human confirmation):**
1. M6 success criteria require the agent loop to continue — that means verifying the full read path through pre-step injection, not just the provider boundary in isolation.
2. Provider maps degraded to EMPTY_RECALL; pre-step skips injection and returns the original decision unchanged.
3. Remex CMIS keys memories by tenant/user headers; DSH session id is not part of the Remex contract, so cross-session recall is same tenant+user across new DSH sessions.

## Implementation summary
- `tests/failure.test.ts`: 503, fetch failure, degraded → EMPTY_RECALL; pre-step unchanged, no throw
- `tests/integration/recall-experiment.test.ts`: Teja/driving/dosa scripted cross-session recall with in-memory Remex stand-in
