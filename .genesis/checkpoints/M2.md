# M2 — Remex HTTP client + unit tests

## G0 · 2026-08-20T18:13+05:30
- wiki pages read: [[concepts/RemexMemoryProvider]], [[concepts/IdentityMapping]], index.md sources (routes_retrieve.py)
- implementation-notes searched for: "remex-client", "identity.ts" — none live (M1 skeleton only)
- codebase grep: `remex-client.ts`, `identity.ts`, `tests/remex-client.test.ts` — 0 hits (unbuilt)
- decisions that bind us: query param (not q), UUID v5 source_turn_ids, timeout on every outbound call
- verdict: **UNBUILT**

## iter 1 · 2026-08-20T18:14+05:30
- gate G1: pass · skills considered [agentic-swe-master, coding-orchestrator, tdd, production-readiness]; chose tdd + production-readiness
- gate G2: pass · +3 src files, +1 test file, 9 tests green
- gate G3: pass · single BUILD iter
- gate G4: pass · demo exit 0:
  ```bash
  pnpm test tests/remex-client.test.ts
  # Test Files  1 passed (1) · Tests  9 passed (9)
  pnpm exec tsc --noEmit  # exit 0
  ```
- gate G5: deferred → L4 below
- next: L4 VERIFY

## L4 VERIFY · 2026-08-20T18:15+05:30

**Context (fresh — no build trail):**
- Goal: Remex HTTP client with health, retrieve (`query` param), evaluate (202 + job_id); mocked fetch tests match FastAPI shapes; timeouts configured; retrieve maps `degraded`; errors throw for M3 provider fail-open.
- Artifact: `src/remex-client.ts`, `src/identity.ts`, `tests/remex-client.test.ts`
- Invariants at risk: `plugin_boundary`, `retrieve_fail_open` (M3 wraps), `write_non_blocking` (timeouts)

**Invariant check:**
| Invariant | Verdict |
|-----------|---------|
| `plugin_boundary` | PASS — imports only `./identity.ts` + stdlib; no DSH/Remex internals |
| `retrieve_fail_open` | PASS (scoped) — client maps `degraded:true`; throws on HTTP errors (M3 provider catches → empty) |
| `write_non_blocking` | PASS — `AbortSignal.timeout(timeoutMs)` on every request; default 5s; evaluate returns jobId without poll |

**Success criteria check:**
| Criterion | Verdict |
|-----------|---------|
| health(), retrieve(), evaluate() implemented | PASS |
| retrieve uses `query` param | PASS (test asserts no `q=`) |
| evaluate expects 202 + job_id | PASS |
| mocked tests match FastAPI shapes | PASS (snake_case in, camelCase out) |
| timeouts configured | PASS (configurable + test) |
| degraded mapped | PASS |
| demo command green | PASS (9/9) |

**Verdict: APPROVE**

**Quiz-me gate (verifier → human):**
1. **Design:** Why does retrieve throw on 503 instead of returning empty memories in the client?
2. **Edge case:** What happens when Remex returns HTTP 200 with `{ memories: [], degraded: true }`?
3. **Impact:** Why is `messageIdToTurnUuid` deterministic UUID v5 instead of `randomUUID()` per call?

**Suggested answers (for human confirmation):**
1. Fail-open belongs at the provider/agent boundary (M3); the client stays a thin HTTP adapter that surfaces transport/API failures distinctly from degraded success responses.
2. Client returns `{ memories: [], tokenCount: 0, degraded: true }` — empty recall with degraded flag preserved for upstream handling.
3. Retries and duplicate evaluate calls must produce the same `source_turn_ids` for idempotent provenance and audit linkage to DSH MessageIds.
