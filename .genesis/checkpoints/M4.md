# M4 — Context injector (retrieve before inference)

## G0 · 2026-08-20T18:25+05:30
- wiki pages read: [[concepts/ContextInjector]], [[concepts/RemexMemoryBlock]], [[concepts/FailOpenRetrieve]]
- implementation-notes searched for: "context-injector", "format-context" — none live
- codebase grep: `context-injector.ts`, `format-context.ts` — 0 hits (unbuilt)
- decisions that bind us: await next() first; fold after claimed batch; dedupe fingerprint; form: recall
- verdict: **UNBUILT**

## iter 1 · 2026-08-20T18:28+05:30
- gate G1: pass · skills considered [agentic-swe-master, tdd, llmops-ai-agents]; chose tdd + llmops-ai-agents
- gate G2: pass · +2 src files, +1 test file, 10 tests green
- gate G3: pass · single BUILD iter
- gate G4: pass · demo exit 0:
  ```bash
  pnpm test tests/context-injector.test.ts  # 10/10
  pnpm exec tsc --noEmit  # exit 0
  ```
- gate G5: deferred → L4 below

## L4 VERIFY · 2026-08-20T18:28+05:30

**Goal:** agent/pre-step injector; await next(); recall from last user message; inject `<remex_memory>`; fold after claimed batch; dedupe on tool continuations.

**Invariant check:**
| Invariant | Verdict |
|-----------|---------|
| `plugin_boundary` | PASS — `@deepseek-ai/cordis`, `@deepseek-ai/dsh-llm`, local `src/**` only |
| `retrieve_fail_open` | PASS — uses `ctx.memory.recall()` which fail-opens in provider |
| `write_non_blocking` | PASS — read-only pre-step path; no evaluate on hot path |

**Success criteria:**
| Criterion | Verdict |
|-----------|---------|
| format-context builds `<remex_memory>` block | PASS |
| await next() before inject | PASS (order test + handler structure) |
| foldAfterClaimed after claimed batch | PASS |
| dedupe fingerprint skips tool continuations | PASS |
| dedupe skips existing block in decision.messages | PASS |
| demo command green | PASS (10/10) |

**Verdict: APPROVE**
