# M7 — memory_search tool + packaging docs

## G0 · 2026-08-20T18:52+05:30
- wiki pages read: index.md (memory_search Phase 7), dsh-mem tool pattern
- implementation-notes searched for: "memory-tools.ts" — none live
- codebase grep: `memory-tools.ts`, patch mount for injector/remember — absent
- decisions that bind us: local ToolRegistrar seam (dsh-tools not on public npm); full patch stack in cordis.patch.yml
- verdict: **UNBUILT**

## iter 1 · 2026-08-20T18:53+05:30
- gate G1: pass · skills considered [agentic-swe-master, tdd, llmops-ai-agents]; chose tdd + llmops-ai-agents
- gate G2: pass · +1 src file, +1 test file, README + patch + exports updated; 7 new tests
- gate G3: pass · single BUILD iter
- gate G4: pass · demo exit 0:
  ```bash
  pnpm test && pnpm exec tsc --noEmit  # 51/51 · tsc exit 0
  grep -q memory-tools cordis.patch.yml  # pass
  ```
- gate G5: deferred → L4 below

## L4 VERIFY · 2026-08-20T18:57+05:30

**Context (fresh — no build trail):**
- Goal: `memory_search` tool via `ctx.memory.recall`; README with env config + `dsh plugin` / local patch; full suite + tsc clean.
- Artifact: `src/memory-tools.ts`, `tests/memory-tools.test.ts`, `README.md`, `cordis.patch.yml`, `package.json` exports
- Invariants at risk: `plugin_boundary`, `retrieve_fail_open` (tool uses same recall seam)

**Computed gates:**
```bash
pnpm test && pnpm exec tsc --noEmit  # exit 0 · 51/51
grep -q memory-tools cordis.patch.yml && grep -q context-injector cordis.patch.yml && grep -q remember cordis.patch.yml  # pass
```

**Invariant check:**
| Invariant | Verdict |
|-----------|---------|
| `plugin_boundary` | PASS — `memory-tools.ts` imports only `@deepseek-ai/cordis` + local `format-context.ts` / `memory.ts` |
| `retrieve_fail_open` | PASS — tool delegates to `memory.recall()` which fail-opens in provider; test covers EMPTY_RECALL |
| `write_non_blocking` | N/A — read-only tool path |

**Success criteria check:**
| Criterion | Verdict |
|-----------|---------|
| `memory_search` registered | PASS — `apply()` → `ctx.tools.register` with `inject: ['memory', 'tools']` |
| Tool calls `ctx.memory.recall` | PASS — `executeMemorySearch` → `memory.recall(query, …)`; test asserts call |
| README env config | PASS — config table + `REMEX_BASE_URL` / `REMEX_TENANT_ID` / `REMEX_USER_ID` |
| README `dsh plugin` + local patch | PASS — published + absolute-path install documented |
| Full patch stack mounted | PASS — provider, injector, remember, memory-tools in `cordis.patch.yml` |
| Demo command green | PASS (51/51 + tsc) |

**Verdict: APPROVE**

**Note:** Uses local `ToolRegistrar` seam instead of `@deepseek-ai/dsh-tools` `defineTool` (private npm dep chain). Raw definitions are accepted by DSH `ctx.tools.register` per harness cookbook.

**Quiz-me gate (verifier → human):**
1. **Design:** Why does `memory_search` call the same `ctx.memory.recall` seam as the pre-step injector instead of hitting Remex HTTP directly?
2. **Edge case:** What does the tool return when recall fail-opens to empty?
3. **Impact:** Why mount injector + remember + tools together in M7's patch rather than leaving them for later?

**Suggested answers (for human confirmation):**
1. Single MemoryService seam keeps fail-open, token budget, and provider swap in one place — tools stay consumers, not adapters.
2. `{ memories: [], tokenCount: 0 }` with no `formatted` block; the tool does not throw.
3. M7 is packaging/DX — a usable plugin ships the full retrieve/inject/remember/search loop in one patch install.

## Implementation summary
- `src/memory-tools.ts`: `memory_search` via `ctx.tools.register`; execute → `ctx.memory.recall`
- `cordis.patch.yml`: mounts provider, context-injector, remember, memory-tools
- `README.md`: install (dsh plugin + local patch), config/env, architecture
- `package.json` exports for all plugin entrypoints
