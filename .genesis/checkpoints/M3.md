# M3 — MemoryService provider + Cordis mount

## G0 · 2026-08-20T18:17+05:30
- wiki pages read: [[concepts/MemoryService]], [[concepts/RemexMemoryProvider]], [[concepts/CordisPatchMount]]
- implementation-notes searched for: "remex-provider", "memory.ts" — none live
- codebase grep: `remex-provider.ts`, `memory.ts` — 0 hits (unbuilt)
- decisions that bind us: fail-open recall at provider layer; Cordis service name `memory`
- verdict: **UNBUILT**

## iter 1 · 2026-08-20T18:19+05:30
- gate G1: pass · skills considered [agentic-swe-master, coding-orchestrator, modular-architecture, tdd]; chose modular-architecture + tdd
- gate G2: pass · +2 src files, patch updated, +1 test file, 4 tests green
- gate G3: pass · single BUILD iter
- gate G4: pass · demo exit 0:
  ```bash
  pnpm test tests/remex-provider.test.ts  # 4/4
  grep -q remex-provider cordis.patch.yml  # ok
  pnpm exec tsc --noEmit  # exit 0
  ```
- gate G5: deferred → L4 below

## L4 VERIFY · 2026-08-20T18:19+05:30

**Goal:** Abstract MemoryService; RemexMemoryProvider registered as Cordis `memory`; patch mounts provider; recall/save delegate to client; fail-open empty recall on errors/degraded.

**Invariant check:**
| Invariant | Verdict |
|-----------|---------|
| `plugin_boundary` | PASS — imports only `@deepseek-ai/cordis` + local `src/**` |
| `retrieve_fail_open` | PASS — `recall()` catches throws + `degraded:true` → `EMPTY_RECALL`, never throws to caller |
| `write_non_blocking` | PASS — `save()` delegates evaluate (202 + jobId); client timeouts unchanged |

**Success criteria:**
| Criterion | Verdict |
|-----------|---------|
| memory.ts abstract service | PASS |
| remex-provider.ts Cordis `memory` service | PASS (`super(ctx, "memory")`) |
| patch references remex-provider | PASS |
| recall/save delegate to client | PASS (4 unit tests) |
| fail-open empty recall | PASS (503 throw + degraded tests) |

**Verdict: APPROVE**
