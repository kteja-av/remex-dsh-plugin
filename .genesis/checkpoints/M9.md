# M9 — Historical retrieve opt-in

## G0 · 2026-08-27
- wiki pages read: [[concepts/RemexMemoryProvider]], [[concepts/AsyncEvaluateWrite]] (candidate match on "retrieve seam")
- implementation-notes searched for: "historical" — none
- codebase grep: `historical` in src/** and tests/** — 0 hits (unbuilt)
- decisions that bind us: retrieve_fail_open, plugin_boundary, write_non_blocking invariants; remex-ai M14 `historical=true` opt-in surface confirmed in `routes_retrieve.py`
- verdict: UNBUILT

## iter 1 · 2026-08-27
- gate G1: pass · skills considered [agentic-swe-master, coding-orchestrator, modular-architecture, production-readiness]; chose production-readiness
- gate G2: pass · +2 source option fields, +1 query-param conditional, +5 tests; provider passes option through unchanged
- gate G3: pass · well under 50000 token budget
- gate G4: pass · `pnpm test tests/remex-client.test.ts tests/remex-provider.test.ts` 18/18 exit 0 · full `pnpm test` 57/57 exit 0 · `pnpm exec tsc --noEmit` exit 0
- gate G5: deferred (independent L4 at exit)
- decision: `historical` is opt-in only; `false`/`undefined` omit the param so active-only default is preserved
- next: L4 VERIFY, then checkpoint/CURRENT/PLAN/notes updates

## Demo evidence · 2026-08-27
- `pnpm test tests/remex-client.test.ts tests/remex-provider.test.ts` exit 0 — 2 files, 18/18
- `pnpm test` exit 0 — 7 files, 57/57
- `pnpm exec tsc --noEmit` exit 0
- live probe against remex-ai M25 (7e4f57b): `/v1/memories:retrieve?...&historical=true` → 200; default query (no `historical`) → 200

## L4 VERIFY (independent agent) · 2026-08-27
- checker: fresh-context general-purpose subagent (separate from maker)
- verdict: APPROVE
- reasoning: freeze boundary holds (M9 product changes confined to the 5 listed files + .genesis bookkeeping); `historical=true` emitted only via strict `=== true` guard; omitted for default and explicit `false`; provider forwards `historical` through `RecallOptions` unchanged; demo 18/18 exit 0, full suite 57/57 exit 0, `tsc --noEmit` exit 0; live backend accepts `historical=true` and default query (both 200)
- invariants: plugin_boundary HELD (zero new imports; no harness-core/remex-ai internals), retrieve_fail_open HELD (try/catch + degraded→EMPTY_RECALL unchanged; new flag only affects query-string construction), write_non_blocking HELD (save/evaluate untouched; fire-and-forget + AbortSignal.timeout intact)
- falsification probes: explicit `historical:false` does not leak into URL; default/undefined omitted; backend `historical=true` vs default both 200; auth-less 422 confirmed as identity validation (environment), not product regression; full suite + typecheck re-run clean

